{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": null,
  "buildCommand": "rm -rf web && cp -r app/build/web web",
  "installCommand": "echo 'no install needed'",
  "outputDirectory": "web",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
