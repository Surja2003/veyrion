<?xml version="1.0" encoding="utf-8"?>
<!--
  Production traffic must be HTTPS. Cleartext (http://) is permitted ONLY for
  local development hosts so the app can reach a backend on your dev machine:
    - 10.0.2.2  : the host machine as seen from the Android emulator
    - localhost / 127.0.0.1 : on-device local servers
  Point the app at your https:// Hugging Face Space for real deployments.
-->
<network-security-config>
    <base-config cleartextTrafficPermitted="false" />
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="true">10.0.2.2</domain>
        <domain includeSubdomains="true">localhost</domain>
        <domain includeSubdomains="true">127.0.0.1</domain>
    </domain-config>
</network-security-config>
